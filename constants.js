// Grid size
export const GRID_SIZE = 8;

// Number of pieces to display in the bank
export const NUM_BANK_PIECES = 3;

// Score for clearing both row(s) and column(s) simultaneously
export const SPECIAL_CLEAR_SCORE = 10;

// Score for clearing a single row or column
export const SINGLE_CLEAR_SCORE = 1;

// URL for the bubble image
export const BUBBLE_IMAGE_URL = './bubble.png';

export const BUBBLE_SIZE = 139;

// Vertical offset for drag and drop
export const VERTICAL_DRAG_OFFSET = 100;
